[1mdiff --git a/components/time/time-entries-list.tsx b/components/time/time-entries-list.tsx[m
[1mindex 20828f3..9653413 100644[m
[1m--- a/components/time/time-entries-list.tsx[m
[1m+++ b/components/time/time-entries-list.tsx[m
[36m@@ -29,11 +29,17 @@[m [mexport function TimeEntriesList({ orgId, userId, projectId, onEdit }: TimeEntrie[m
 			if (projectFilter !== 'all') params.append('project_id', projectFilter);[m
 			if (statusFilter !== 'all') params.append('status', statusFilter);[m
 [m
[31m-			const response = await fetch(`/api/time/entries?${params.toString()}`);[m
[31m-			if (!response.ok) throw new Error('Failed to fetch time entries');[m
[31m-			[m
[31m-			const data = await response.json();[m
[31m-			return data.entries as TimeEntryWithRelations[];[m
[32m+[m		[32mconst response = await fetch(`/api/time/entries?${params.toString()}`);[m
[32m+[m		[32mif (!response.ok) throw new Error('Failed to fetch time entries');[m
[32m+[m[41m		[m
[32m+[m		[32mconst data = await response.json();[m
[32m+[m		[32m// Ensure entries is always an array, never null/undefined[m
[32m+[m		[32mconst entries = Array.isArray(data?.entries) ? data.entries : [];[m
[32m+[m		[32m// Ensure each entry has work_order property (null if missing)[m
[32m+[m		[32mreturn entries.map((entry: any) => ({[m
[32m+[m			[32m...entry,[m
[32m+[m			[32mwork_order: entry.work_order ?? null[m
[32m+[m		[32m})) as TimeEntryWithRelations[];[m
 		},[m
 		staleTime: 30 * 1000,       // 30 seconds (time entries change frequently)[m
 		gcTime: 5 * 60 * 1000,       // 5 minutes[m
